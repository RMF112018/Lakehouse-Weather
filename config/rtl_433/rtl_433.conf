# Gate-1 / diagnostic configuration.
# Start broad. Once the station is identified, filtering happens by model + sensor ID.
frequency 915M
output json
report_meta time:utc
report_meta protocol

# Do not add a device ID filter here until Gate 1 captures the actual WH65B ID.
